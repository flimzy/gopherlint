version: "2"

formatters:
  enable:
    - gci
  settings:
    gci:
      sections:
        - standard
        - prefix(github.com/flimzy/gopherlint,github.com/daixiang0/gci)
        - default
      custom-order: true
